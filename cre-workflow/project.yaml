# ==========================================================================
# CRE PROJECT SETTINGS
# ==========================================================================
# Keyed by target. `cre workflow <cmd> --target <name>` selects one.
# RPC URLs support ${VAR} syntax, resolved from cre-workflow/.env or the shell,
# which keeps credentialled endpoints out of this committed file.
# ==========================================================================

staging-settings:
  rpcs:
    # Base Sepolia: GrantEscrow and its SettlementReceiver live here, and the
    # settlement workflow reads and writes here.
    - chain-name: ethereum-testnet-sepolia-base-1
      url: https://sepolia.base.org
    # Workflow Registry chain — where a deployed workflow is registered.
    - chain-name: ethereum-testnet-sepolia
      url: https://ethereum-sepolia-rpc.publicnode.com

# ==========================================================================
production-settings:
  rpcs:
    - chain-name: ethereum-testnet-sepolia-base-1
      url: https://sepolia.base.org
    - chain-name: ethereum-testnet-sepolia
      url: https://ethereum-sepolia-rpc.publicnode.com
